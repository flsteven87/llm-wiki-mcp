---
name: wiki-ingest
description: Ingest a new source (article, paper, URL, transcript, file, pasted text) into an existing Karpathy LLM wiki. Reads the source, surfaces takeaways, writes a summary page, updates entity and concept pages, audits backlinks, updates the index, and appends a log entry. Use whenever the user gives you a URL, file path, or pasted document and asks to "add this to the wiki", "ingest", "process", or simply pastes a source into a wiki context.
---

# wiki-ingest

> **Status:** stub. Full content deferred to a separate skill-content plan after the MCP framework is stable.

---
name: wiki-update
description: Apply a targeted update to an existing wiki page when new information arrives that doesn't warrant a full ingest — correcting a fact, adding a recent development, refreshing a stale claim, or restructuring a single page. Use when the user says "update the X page", "the Y page is wrong", "add this detail to Z", and is naming a specific existing page rather than ingesting a new source.
---

# wiki-update

> **Status:** stub. Full content deferred to a separate skill-content plan after the MCP framework is stable.
